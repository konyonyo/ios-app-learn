# Family AI 設計書

## 1. 目的

自分と家族が使う、端末内の個人データを活用できるシンプルなチャットボットを作る。

このアプリは、Hermes AgentやPi Agent全体をiOSへ移植することを目的としない。高性能なLLMエンドポイントを利用し、iOSアプリ側では会話・記憶・プロンプトを管理する。

## 2. 対象ユーザー

- 自分
- 家族

会話履歴は各ユーザーの端末内だけに保存する。端末間で会話履歴を共有する機能は、初期版では提供しない。

## 3. 初期版の範囲

### 対応するデータ

- `SOUL.md`
- `USER.md`
- `MEMORY.md`
- ChatGPTエクスポートデータ
- アプリ内の会話履歴

### 初期版で行うこと

- チャット画面を表示する
- ユーザーの入力を受け取る
- 現在の会話履歴を端末内に保存する
- 必要な記憶を端末内で検索する
- システムプロンプトと会話コンテキストを組み立てる
- LLMエンドポイントへHTTPSリクエストを送る
- LLMの回答を表示する

### 初期版で行わないこと

- Hermes Agentの組み込み
- Pi Agentの組み込み
- カレンダーやリマインダーへのアクセス
- ファイル操作やコマンド実行
- 外部サービスの操作
- サーバの運用
- 家族間の会話履歴同期
- 端末内LLMによる推論

## 4. セットアップとデータ入力

初回起動時はセットアップウィザードを表示する。

```text
1. SOUL.mdを設定
2. USER.mdを設定
3. MEMORY.mdを設定
4. ChatGPTエクスポートをインポート
5. チャットを開始
```

### 4.1 SOUL / USER / MEMORY

ユーザーがChatGPTを利用していることを前提とし、アプリは各Markdownを生成するためのプロンプトを表示する。

```text
アプリでプロンプトをコピー
        ↓
ChatGPTへ貼り付け
        ↓
ChatGPTの生成結果をコピー
        ↓
アプリへ貼り付け
        ↓
確認・編集して保存
```

3ファイルは別々に生成する。生成結果はユーザーが確認・編集してから保存する。アプリからChatGPT APIは呼び出さない。

- `SOUL.md`: ボットの人格、口調、回答方針
- `USER.md`: ユーザーの基本情報、好み、環境
- `MEMORY.md`: 長期的に保持したい事実や前提

### 4.2 ChatGPTエクスポート

iOSのファイル選択画面からChatGPTの`.zip`を選択する。

```text
ZIPを選択
    ↓
アプリSandboxへコピー
    ↓
conversations.jsonを抽出
    ↓
SQLiteへ変換
    ↓
全文検索インデックスを作成
```

インポート画面には、会話数、メッセージ数、スキップ数、インデックス作成状態を表示する。ZIP全体をLLMへ送信してはいけない。インポート後は、元ZIPをアプリ内から削除できるようにする。

実装は、まず`conversations.json`の読み込みを行い、その後ZIPの直接インポートを追加する。

## 5. 基本アーキテクチャ

```text
┌──────────────────────────────────────┐
│ iOSアプリ                             │
│                                      │
│  SwiftUI                             │
│    └─ ChatView                       │
│                                      │
│  アプリケーション層                  │
│    ├─ ChatStore                      │
│    ├─ ContextBuilder                 │
│    ├─ MemorySearch                   │
│    └─ LLMClient                      │
│                                      │
│  端末内ストレージ                    │
│    ├─ 会話履歴                       │
│    ├─ 記憶データ                     │
│    └─ ChatGPTエクスポート             │
└──────────────────┬───────────────────┘
                   │ HTTPS
                   ▼
             LLM API endpoint
```

サーバは構築しない。iOSアプリからLLMエンドポイントへ直接HTTPS通信する。

## 6. データの扱い

### 5.1 常用コンテキスト

小さなテキストとして、基本的には毎回コンテキストへ含める。

- `SOUL.md`: ボットの人格・振る舞い
- `USER.md`: ユーザーに関する基本情報

`MEMORY.md`は小さい間は毎回含める。大きくなった場合は検索対象へ移す。

### 5.2 検索対象データ

毎回すべてをLLMへ送信せず、質問に関連するものだけを検索して送信する。

- ChatGPTエクスポートデータ
- 古いアプリ内会話履歴
- 大きくなった`MEMORY.md`

### 5.3 LLMへ送るコンテキスト

```text
System:
  SOUL.md
  USER.md
  必要に応じてMEMORY.md

Relevant memory:
  ローカル検索で得られた関連情報

Conversation:
  現在の会話の直近履歴
  必要に応じて会話要約

User:
  今回の入力
```

記憶データは命令ではなく、参照用データとして扱う。記憶内に命令文が含まれていても、アプリやLLMがそれをシステム命令として実行しないよう、プロンプト上で明確に分離する。

## 7. ローカルストレージ

初期版のセッション履歴には、iOS標準のSwiftDataを使用する。SwiftDataのデータはアプリSandbox内に保存され、アプリ再起動後も復元される。

ChatGPTエクスポートや将来の知識検索では、別途SQLiteとFTS5を使用する。会話履歴と知識データは論理的に分離する。

```text
SwiftData
├── ChatSession
└── ChatMessage

SQLite FTS5（Phase 4以降）
├── knowledge_documents
├── knowledge_chunks
└── knowledge_chunks_fts
```

想定する役割:

- `conversations`: 会話単位の情報
- `messages`: user / assistantのメッセージ本文
- `messages_fts`: 全文検索用インデックス
- `knowledge_documents`: SOUL / USER / MEMORY / ChatGPT履歴などの文書情報
- `knowledge_chunks`: 検索単位に分割した記憶データ
- `knowledge_chunks_fts`: 記憶データの全文検索用インデックス

ChatGPTエクスポートデータも、アプリ内会話履歴とは論理的に分離する。検索実装はSQLite FTS5から始める。検索品質が不足した場合に、将来意味検索を追加する。

## 8. セッション

セッションはアプリ内の1つの会話単位を表す。セッションは端末内だけに保存し、初期版では端末間同期を行わない。

```text
セッション一覧
├── iOSアプリ開発
├── 家族の予定
├── 旅行の相談
└── 雑談
```

各セッションは、ID、タイトル、作成日時、更新日時、メッセージ一覧を持つ。初期版では、新規セッション、セッション一覧、セッション切り替え、アプリ再起動後の復元を実装する。

アプリ内会話履歴とChatGPTエクスポートは別のデータソースとして管理する。

## 9. 会話コンテキスト管理

過去の全会話を毎回送信しない。

```text
直近の会話履歴
  + 古い会話の要約
  + ローカル検索で得た関連情報
```

会話が長くなった場合は、古い部分を要約して保存する。要約処理は必要になってから追加する。

### 9.1 コンテキスト作成手順

```text
1. 現在のセッションを取得
2. 直近のメッセージを取得
3. SOUL / USER / MEMORYを取得
4. ChatGPT履歴をローカル検索
5. アプリ内の過去セッションをローカル検索
6. 検索結果を重複除去・切り詰め
7. LLM用messagesを構築
8. LLMへ送信
9. 回答を現在のセッションへ保存
```

初期版では、SOULとUSER、比較的小さいMEMORYを常用コンテキストとし、ChatGPT履歴と古い会話は検索で得られた関連部分だけを追加する。

```text
常用: SOUL.md + USER.md + MEMORY.md
直近: 現在のセッションの直近メッセージ
検索: ChatGPT履歴 + 古いアプリ内会話
```

## 10. LLMクライアント

固定応答版から実際のLLM接続へ移行しやすくするため、LLM呼び出しを画面コードから分離する。

```text
protocol LLMClient {
    func send(messages: [ChatMessage]) async throws -> String
}
```

実装は段階的に差し替える。

```text
FixedResponseClient
        ↓
HTTP LLMClient
```

HTTP通信には`URLSession`を使用する。初期版ではストリーミング対応を必須にしない。通常のレスポンス取得が動いてから、必要に応じて追加する。

## 11. 認証情報とプライバシー

- APIキーはソースコードに埋め込まない
- APIキーはiOS Keychainへ保存する
- エンドポイントURLやモデル名はアプリ設定として管理する
- 通信はHTTPSのみとする
- ローカルデータはアプリSandbox内に保存する
- LLMへ送信するデータは必要最小限にする

サーバなし構成では、APIキーを端末から完全に秘匿することはできない。初期版は家族内利用を前提とし、一般公開向けの認証設計は別途検討する。

## 12. 開発フェーズ

### Phase 1: 現在完了

- SwiftUIチャット画面
- 固定応答
- ユーザーメッセージ表示
- ボットメッセージ表示

### Phase 2: LLM接続（基盤実装済み）

- `LLMClient`プロトコルを導入
- 固定応答用の`FixedResponseClient`を実装
- OpenAI互換形式を想定した`OpenAICompatibleClient`を実装
- URLSessionによるHTTPSリクエスト処理を実装
- HTTPエラーと空レスポンスの処理を実装
- 現在のアプリはエンドポイント未設定のため`FixedResponseClient`を使用
- APIキーをKeychainへ保存
- `OpenAI-Organization`ヘッダー対応
- 設定画面とKeychain保存を実装済み
- エンドポイント未設定時は固定応答を使用する
- 実際のエンドポイント接続は未実施

### Phase 3: セッションと基本コンテキスト（実装中）

- SwiftDataで会話履歴を端末内に保存
- アプリ再起動後も会話を表示する
- 新規セッションを作成する
- セッション一覧と会話詳細を分ける
- `SOUL.md`をアプリへ取り込む
- `USER.md`をアプリへ取り込む
- 現在の会話履歴をリクエストへ含める

### Phase 4: ChatGPTエクスポート

- `conversations.json`を読み込む
- エクスポートデータをアプリへ取り込む
- SQLiteへ変換する
- FTS5で検索する
- 検索結果をLLMコンテキストへ追加する

### Phase 5: ZIPインポートと品質改善

- ZIPファイルを直接インポートする
- `MEMORY.md`の扱いを追加
- 会話の要約を追加
- 検索結果の件数・長さを調整する
- ストリーミング表示を追加する
- エラー表示と再送を改善する

## 11. 成功条件

初期版の成功条件は、次のとおりとする。

1. 家族がチャット画面から質問できる
2. LLMから回答が返る
3. SOUL / USERの内容が回答に反映される
4. アプリを再起動しても自分の会話履歴が残る
5. ChatGPTエクスポートの関連情報を検索できる
6. 必要な関連情報だけをLLMへ送信できる
7. PiやHermesをアプリへ組み込まずに動作する

## 12. 参考

- ChatGPT履歴のHermes移植方式: https://github.com/konyonyo/chatgpt2hermes
- Pi Agent Harness: https://github.com/badlogic/pi-mono
